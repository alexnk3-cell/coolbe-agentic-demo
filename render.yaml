services:
  - type: web
    name: pdd-kpi-system
    env: python
    rootDir: kpi-system
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
